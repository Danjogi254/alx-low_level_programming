#include <stdio.h>
/**
 * main - Starting point
 * Description: 'Numbers'
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n <= 0; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
